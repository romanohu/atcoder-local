//#include <bits/stdc++.h>
#include <iostream>
#include <string>
#include <vector>
using namespace std;
using ll = long long;

#define rep(i, N) for (int i=0; i<(N); ++i)

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int M;
    cin >> M;

    vector<int> S1(M), S2(M), S3(M);
    rep(i, M) cin >> S1[i];
    rep(i, M) cin >> S2[i];
    rep(i, M) cin >> S3[i];

    


    return 0;
}
